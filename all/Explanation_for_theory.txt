1. В конце каждой ссылки где чтото связано с человеком,как я заметил,ВСЕГДА есть id  этого человека,в бд можно записывать по нему
В моем понимании это даже не бд а json файл,тк бд скачивать долго и большая нагрузка с серверами.
он будет выглядеть как:
{
 id,
 full_name,
 any_information,
 last data,
 pre-last data,
 etc
}

2. При запросе к ии,также даст ему данные из этого файла,используя айди
3. Таким образом,ты спуста не сможешь дать ему какие либо доступы
4. Также,для чего либо,стоить дать иишке пути и кнопки по которым надо идти для хода,в +- таком формате:
steps:
[
  {
   "page" : str,
   "button" : selector
  },
  {
   "page2" : str,
   "button2" : selector
  }
]

Таким образом ии будет знать как куда идти,но лучше для сохранения токенов дать ему просто названия страниц и путь к нему,или вообще запускать 2 ии для 
получения доступа, тогда и будет тратиться около 700-800 токенов сверху(вместо предполагаемых 3000) ,тк ему не нужен будет сканер и лишняя нагрузка, и и не запутается.



{
Для решения проблем выбора страниц:
Сначало проверим страницу,она должна быть   https://hospital-akt.dmed.kz/medicalHistory/medicalHistory
Затем пользуемся document.getElementsByClassName('mh-navigation nav nav-pills big-screen')[0].getElementsByClassName('active')[0].classList[0]
И узнаем его класс
если это -- то пишем --
И так проще будет искать

Конечно,если сайт дамумеда обновят,то нам пизда,но пока все ок

}Это все для сканера
